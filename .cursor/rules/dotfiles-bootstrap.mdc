---
description: Bootstrap, chezmoi lifecycle, and dependency management for mydotfiles
alwaysApply: true
---

# Dotfiles / bootstrap (agent)

Human docs: `README.md`. WSL: `wsl-bootstrap.mdc`.

## Architecture
- Declarative first (`dot_*`, `private_*`, `dot_config/...`); scripts last.
- `run_once_*` / `run_onchange_*`: small, idempotent; packages/migrations only.
- One bootstrap style: `bootstrap.ps1` / `bootstrap.sh` → tool → init/apply|update → optional SSH. Local checkouts should link `~/.local/share/chezmoi` → the repo (junction/symlink) so bare `chezmoi apply` works without `--source`.
- Ship Linux + Windows unless OS-specific; templates only for real diffs.
- Migrations that touch existing user files must backup first and be `run_once_*`. SSH migrate must be CRLF-safe and **never** copy an `Include config.d/...` into `config.d/private.conf` (infinite recursion).
- Profiles soft-fail on missing/mismatched bins — never `eval` broken init.
- Ensure `~/.local/bin` on PATH before relying on user-installed tools.

## Dependencies / ABI
- Pin self-installed bins in `.chezmoidata.toml`; float apt (`zsh`, `fzf`, …).
- Preferred + floor when PPAs lag: `git` + `git_minimum` (≥ 2.35 / `zdiff3`). Ubuntu 20.04 git-core PPA ~2.50 max.
- Pins: `chezmoi`, `oh_my_posh`, `mise`, `just`, `python` (mise global default), `zoxide`, `ripgrep`, `fd`, `git_delta`, `git`, `git_minimum` — bump in data file only.
- Portable Cursor User Rules live as a **paste source** in `agent/cursor-user-rules.md` (Cursor does not load `~/.cursor/rules` as global User Rules). Antigravity global: managed `~/.gemini/GEMINI.md`.
- `mise` is scaffolding only: install + soft-fail activate + global `python` pin. On Windows, install **both** `mise.exe` and `mise-shim.exe` side-by-side (native shim mode). Flipper/ufbt/firmware/dev-board hosts stay in separate project repos (their own `mise.toml`).
- Activate mise in **PowerShell 7+ only**; on Windows PowerShell 5.1 skip activate and set `MISE_PWSH_CHPWD_WARNING=0` (mise chpwd is unsupported there).
- New pin: data → installer → soft-fail guard → CI asset check.
- **glibc floor:** Go/CGO release builds from Ubuntu 22.04+ need `GLIBC_2.32`/`2.34`. Ubuntu 20.04 (2.31) fails with `version GLIBC_2.3x not found`. Prefer/detect musl (static) or older-compatible assets; do not assume `*-linux-amd64` / `*-glibc*` runs everywhere.
- Chezmoi amd64: musl on musl hosts **or** when host glibc < 2.32; else `linux-glibc`. Verify `--version` after extract; retry musl on linker errors.
- Downloads: winget/apt when exact; else GitHub → `$HOME/.cache/...`, retry, checksum, `~/.local/bin`. No bare `curl | sh` for critical bins on flaky nets.
- Re-runnable: skip if desired version already present.

## Validation / debug
- `bash ./scripts/ci-check.sh`. CI: shellcheck, chezmoi dry-run, templates, pin URLs, PS parse.
- Outer “bootstrap failed” = child exit only — `bash -x` / PS trace in-target.

## Backlog
- Later: optional PM `IdentityAgent`; richer fzf helpers; `navi` if `dots` outgrows a static sheet; `direnv` only if mise env files prove insufficient.
- Skip: plugin frameworks, package sprawl, in-repo keys, secret sync, Flipper/firmware tooling in this repo.
